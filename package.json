{
  "scripts": {
    "build": "npm run build:firefox && npm run build:chrome",
    "build:chrome": "xt-build --env prod --platform chrome --config chrome.xtbuild.json",
    "build:firefox": "xt-build --env prod --platform firefox --config firefox.xtbuild.json"
  },
  "devDependencies": {
    "extension-cli": "^1.2.4"
  }
}
