{
  "scripts": {
    "build": "npm run build:firefox && npm run sign:firefox",
    "build:firefox": "web-ext build --source-dir ./src --artifacts-dir ./dist",
    "sign:firefox": "web-ext sign --channel=unlisted --api-key $MOZILLA_API_KEY --api-secret $MOZILLA_API_SECRET"
  },
  "devDependencies": {
    "web-ext": "^6.7.0"
  }
}
